// Aggregates every module's Drizzle schema so drizzle-kit (and the db
// client) has a single entry point to discover all tables from.
export * from '../../modules/master-data/infrastructure/database/schema.js';
