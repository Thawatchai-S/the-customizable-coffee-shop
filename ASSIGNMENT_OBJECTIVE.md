# Step 3 ข้อที่ 1 — A High-Level Architecture Diagram

แผนภาพสถาปัตยกรรมระดับสูง (คร่าว ๆ) ของระบบร้านกาแฟที่ปรับแต่งได้ แสดงภาพรวม
ว่าแต่ละส่วนของระบบเชื่อมต่อกันอย่างไร โดยไม่ลงรายละเอียดระดับคลาส

```
┌───────────────────────────┐
│      Frontend (Web)        │
│  React 19 + Vite + Tailwind│
│  apps/web                  │
└──────────────┬─────────────┘
               │ HTTP (REST /api/*)
               ▼
┌─────────────────────────────────────────────────────────┐
│                     Backend API (Express 5)               │
│                        apps/api                            │
│                                                             │
│  ┌───────────────────────────────────────────────────┐   │
│  │  infrastructure  — Express routes/controllers,      │   │
│  │  Drizzle repositories, request validation (valibot)  │   │
│  └───────────────────────┬───────────────────────────┘   │
│                           │ เรียกใช้                       │
│  ┌───────────────────────▼───────────────────────────┐   │
│  │  application — use-cases ที่ประสานงาน                │   │
│  │  (BuildDrink, PlaceOrder, GetMasterData)             │   │
│  └───────────────────────┬───────────────────────────┘   │
│                           │ เรียกใช้                       │
│  ┌───────────────────────▼───────────────────────────┐   │
│  │  domain — entity / business rule ล้วน ๆ              │   │
│  │  (Drink, DrinkBuilder, Order, PricingCalculator ฯลฯ)  │   │
│  │  ไม่ผูกกับ framework หรือฐานข้อมูลใด ๆ                 │   │
│  └───────────────────────────────────────────────────┘   │
│                                                             │
│   จัดกลุ่มตามฟีเจอร์ (feature module) 3 กลุ่ม:               │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│   │ master-data │  │   drinks    │  │   orders    │        │
│   │ (เบส/ไซรัป/  │◀─│ (ประกอบ      │◀─│ (สั่งออเดอร์   │        │
│   │ ท็อปปิ้ง/ไซซ์) │  │ เครื่องดื่ม)  │  │ + คิดราคา)   │        │
│   └─────────────┘  └─────────────┘  └─────────────┘        │
└──────────────┬──────────────────────────────────────────┘
               │ Drizzle ORM (SQL)
               ▼
┌───────────────────────────┐
│        PostgreSQL           │
│  ตาราง master data           │
│  (base drinks, syrups,       │
│   toppings, sizes)           │
└───────────────────────────┘
```

## คำอธิบายสั้น ๆ ประกอบแผนภาพ

- **Frontend ↔ Backend**: เว็บฝั่ง React เรียก REST API ของ Express ผ่าน
  `/api/*` เท่านั้น ไม่คุยกับฐานข้อมูลตรง ๆ
- **แบ่งเป็น 3 ชั้นในฝั่ง backend**: `infrastructure` (ต่อกับโลกภายนอก เช่น
  HTTP/DB), `application` (use-case ที่ประสานงาน), `domain` (กฎธุรกิจล้วน ๆ
  ไม่รู้จัก framework) — ทำให้เปลี่ยนเทคโนโลยีฝั่งใดฝั่งหนึ่งได้โดยไม่กระทบ
  ฝั่งอื่น
- **แบ่งเป็น 3 โมดูลตามฟีเจอร์**: `master-data` เก็บข้อมูลตั้งต้นของร้าน,
  `drinks` ใช้ข้อมูลนั้นมาประกอบเครื่องดื่มหนึ่งแก้ว, `orders` ใช้ `drinks`
  ซ้ำเพื่อประกอบหลายแก้วเป็นออเดอร์เดียวพร้อมคิดราคารวม
- **ฐานข้อมูล**: มีแค่ Postgres เก็บ master data เท่านั้น (เบสกาแฟ ไซรัป
  ท็อปปิ้ง ไซซ์) ส่วนเครื่องดื่ม/ออเดอร์ยังเป็นการประมวลผลแบบ in-memory ต่อ
  1 request ยังไม่ persist ลงฐานข้อมูล

---

# Step 3 ข้อที่ 2 — The Choice of Programming Language

**ภาษาที่เลือกใช้ทั้งระบบ: JavaScript** (Backend เป็น Node.js ล้วน, Frontend
เป็น JavaScript/JSX ผ่าน React)

- **Backend — Node.js + Express 5**: เลือกเพราะ Express เบา ไม่บังคับ
  โครงสร้างตายตัว จึงจัดเป็น Clean Architecture (domain/application/
  infrastructure) ได้อิสระตามที่ต้องการ อีกทั้ง Node.js เขียน REST API และ
  ต่อฐานข้อมูลผ่าน ORM (Drizzle) ได้เร็ว เหมาะกับสเกลของระบบนี้
- **Frontend — React 19 + Vite**: เลือกเพราะเป็น library UI ที่นิยม มี
  component model ที่เหมาะกับ UI แบบปรับแต่งเครื่องดื่ม (เลือกเบส/ไซรัป/
  ท็อปปิ้ง/ไซซ์แบบโต้ตอบ) และ Vite ให้ dev server ที่เร็ว

---

# Step 3 ข้อที่ 3 — Benefits: Maintainability & Extensibility

**Backend**

- **เพิ่มวัตถุดิบใหม่โดยไม่แก้โค้ด**: `Ingredient` เป็นโมเดลเดียวขับเคลื่อน
  ด้วยข้อมูล เพิ่มไซรัป/ท็อปปิ้งใหม่แค่เพิ่มแถวใน master data ไม่ต้อง deploy
  โค้ดใหม่และไม่เพิ่มจำนวนคลาส
- **เปลี่ยนกฎราคาได้โดยไม่กระทบส่วนอื่น**: `PricingCalculator` เป็น interface
  กลาง สลับ implementation ใหม่ได้ทันทีโดย `Order`/`Drink` ไม่ต้องแก้เลย
- **แยกชั้นชัดเจน (domain/application/infrastructure)**: แก้ไข/เทส
  business logic ได้โดยไม่ต้องพึ่ง Express หรือฐานข้อมูลจริง ทำให้ดูแลรักษา
  และเพิ่มฟีเจอร์ใหม่ในอนาคตง่ายและปลอดภัยกว่า

**Frontend**

- **จัดโค้ดตาม feature (`features/drinks`, `features/orders`)**: สอดคล้อง
  กับโมดูลฝั่ง backend ทำให้หาไฟล์และแก้ไขฟีเจอร์ใดฟีเจอร์หนึ่งได้ตรงจุด
  ไม่กระทบฟีเจอร์อื่น
- **UI ขับเคลื่อนด้วยข้อมูลจาก `/api/master-data`**: รายการเบส/ไซรัป/
  ท็อปปิ้ง/ไซซ์ไม่ได้ hardcode ไว้ในหน้าเว็บ เพิ่มวัตถุดิบฝั่ง backend แล้ว
  ขึ้นในหน้าเลือกเครื่องดื่มทันทีโดยไม่ต้องแก้ frontend
- **คอมโพเนนต์ใช้ซ้ำได้ (เช่น `QuantityControl`)**: เพิ่ม ingredient ใหม่กี่
  ตัวก็ยังใช้ UI ควบคุมจำนวนตัวเดิม ไม่ต้องเขียนคอมโพเนนต์ใหม่ต่อวัตถุดิบ
